# Terraform state and cache
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan

# Lambda build artefacts — generated by null_resource pip install at apply time
build/
lambda_function.zip
